python -m venv venv
./venv/Scripts/activate
pip install -r requirements.txt
python get_semid.py (optional)
python watchdog.py

# -------------------------------------- dev notes --------------------------------------

python -m handlers.get_html
copy ./test.json ./static/last_state.json ; python watchdog.py
   
pip freeze > requirements.txt
Getting the cookies from VTOP:
- CSRF: doStudentMarkView -> Payload
- JSESSIONID: doStudentMarkView -> Headers -> Request Headers -> Cookie (Beginning)
- SERVERID: doStudentMarkView -> Headers -> Request Headers -> Cookie (End)

Authentication req:
- REGD
- PASS
- CAPTCHA

list of semesters and their mapping: StudentTimeTableChn
Grades
GradeHistory
AcedemicCalender

tests:
- marks removed
- marks added
- marks modified
- grade removed
- grade added
- grade modified
- cgpa modified
- calendar modified


grades returns Error in the beginning of the semester when no grades are released. Probably the same with marks too.

1..20 | ForEach-Object { (Measure-Command { python main.py ; Start-Sleep -Seconds 5 }).TotalSeconds } | Measure-Object -Average -Minimum -Maximum -Sum

pyinstaller --onefile --name watchpup  --add-data "handlers\weights.json;handlers" watchdog.py
pyinstaller --onefile --name helper  --add-data "handlers\weights.json;handlers" get_semid.py

./dist/./watchpup.exe

bugs:
- false notification firing


exe release
- exe file
- setup file for getting the semester
- .env.example


git push origin exec

stupid bug: every time the actions workflow starts, it starts in a new system, and the last_state.json is re initialised, never being compared with the true last_state.json, and always being initialised on every run, essentially giving no notification because for it, the changes never actually happened.